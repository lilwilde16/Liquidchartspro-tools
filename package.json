{
  "name": "liquidchartspro-tools",
  "version": "1.0.0",
  "description": "LiquidChartsPro personal trading tools — autotrader, strength meter, and backtest engine.",
  "private": true,
  "scripts": {
    "test": "jest --coverage",
    "test:watch": "jest --watch",
    "start": "npx serve . -l 8080"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "jest-environment-jsdom": "^29.7.0"
  },
  "jest": {
    "testEnvironment": "jsdom",
    "testMatch": [
      "**/src/tests/**/*.test.js"
    ],
    "transform": {}
  }
}
